version: 1
name: "Step 2 — Create a list under a section"
widgets:
  - id: sectionName
    type: text
    label: "Section name (e.g., projects)"
    # → vars.sectionName

  - id: listKey
    type: text
    label: "List key to add (e.g., items)"
    # → vars.listKey

  - id: go
    type: action
    label: "Create list under section"
    options: { action: "create-list-under-section" }

layout:
  type: vertical
  children: [sectionName, listKey, go]
