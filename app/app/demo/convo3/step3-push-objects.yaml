version: 1
name: "Step 3 — Push objects into list"
widgets:
  - id: listPath
    type: text
    label: "List JSON Pointer (e.g., /projects/items)"
    # → vars.listPath

  - id: json
    type: text
    label: "Object JSON (e.g., { \"id\":1, \"name\":\"Item A\" })"
    # → vars.json

  - id: go
    type: action
    label: "Push object to list"
    options: { action: "push-object-to-list" }

layout:
  type: vertical
  children: [listPath, json, go]
