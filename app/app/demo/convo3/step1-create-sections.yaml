version: 1
name: "Step 1 — Create sections"
widgets:
  - id: sectionsCsv
    type: text
    label: "Sections (comma-separated)"
    # no binding → captured into vars.sectionsCsv

  - id: go
    type: action
    label: "Create sections"
    options: { action: "create-sections" }

layout:
  type: vertical
  children: [sectionsCsv, go]
