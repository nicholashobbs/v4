version: 1
title: "Convo4 — Contact Builder"

initial:
  doc: {}

steps:
  - template: "./step1-name.yaml"
    mode: "diff"

  - template: "./step2-choose-fields.yaml"
    mode: "diff"

  - template: "./step3-fill-chosen.yaml"
    mode: "diff"   # <-- change this from "explicit" to "diff"

  - template: "./step4-create-sections.yaml"
    mode: "diff"

  - template: "./step5-section-hub.yaml"
    mode: "diff"
